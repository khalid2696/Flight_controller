#include "mbed.h"

Serial pc(USBTX,USBRX);
Serial tele(PC_6, PC_7);
int getGains(int meanThr, float *gainArray);

// int  main()
// {
//   pc.baud(57600);
//   tele.baud(57600);
//   pc.printf("Telemetry code starting");
//    while(1){
//   //           pc.printf("potato_PC_comm\n");
//   //           tele.printf("potato_wireless_comm\n");
//   //           wait(0.5);
//   //
//   float gainArray[] ={1,2,3,4,5,6,7,8,9};
//   getGains(0.01, gainArray);
//   pc.printf("\ngains are as follows ");
//   for (int i =0 ;i<9;i++){
//     pc.printf("%f ",gainArray[i]);
//   }
//   wait(0.4);
// }
// return 0;
// }
//
// int getGains(int meanThr, float gainArray[])
// {
//   //NOTE that this code can block the execution for a while.
//   //Donot run when armed
//   if(meanThr>=0.02) return -1; //Check for throttle condition(scaled)
//     pc.printf("potato\n");
//   if(tele.readable() == 0) return -2; //dont run unless we have something to read
//     pc.printf("half\n");
//        if(tele.getc() == '#')  //Start byte
//           { if(tele.getc() == '1') //read gains command !!
//             pc.printf("half done\n");
//             { for(int i = 0; i<9;i++){
//                 while(!tele.readable()) ; //wait for data
//                 pc.scanf("%f\n", &gainArray[i]);
//                 }
//             }
//           }
//   return 0;
// }

int main()
{
  pc.baud(57600);
  tele.baud(57600);
  while(1){
  //pc.printf("%s \n", tele.getc());
  pc.printf("%d\n", pc.readable());
  wait(0.3);
}
}
